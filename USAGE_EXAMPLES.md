# 篮球计分板识别使用示例

## 数据准备

### 数据组织方式（推荐）

**使用文件夹名作为标签**，同一标签的多个图片放在同一文件夹中。这种方式更加灵活，便于管理多个相同标签的图片。

### 目录结构示例

```
data/
├── train/
│   ├── 123/              # 文件夹名是标签"123"（比分）
│   │   ├── img001.jpg
│   │   ├── img002.jpg
│   │   ├── img003.jpg
│   │   └── ...
│   ├── 045/              # 标签"045"（比分）
│   │   └── ...
│   ├── 24/               # 标签"24"（24秒倒计时）
│   │   ├── shot001.jpg
│   │   └── ...
│   ├── 34:56/         # 标签"34:56"（时间）
│   │   └── ...
│   ├── 34:56.789/     # 标签"34:56.789"（时间含毫秒）
│   │   └── ...
│   ├── 第一节/           # 标签"第一节"（中文节次）
│   │   ├── period001.jpg
│   │   └── ...
│   ├── 加时赛/           # 标签"加时赛"（中文节次）
│   │   ├── period001.jpg
│   │   └── ...
│   ├── 第1节/            # 标签"第1节"（数字中文节次）
│   │   └── ...
│   ├── FIRST/            # 标签"FIRST"（英文节次）
│   │   └── ...
│   ├── 1st/              # 标签"1st"（缩写节次）
│   │   └── ...
│   └── ...
└── test/
    ├── 100/
    │   └── ...
    ├── 15/
    │   └── ...
    └── ...
```

### 支持的标签格式

文件夹名（即标签）支持以下格式：

1. **比分**（3位数字）：
   - `123`, `045`, `999`

2. **24秒倒计时**（2位数字）：
   - `24`, `15`, `05`

3. **倒计时**：
   - 分秒：`34:56`
   - 分秒+毫秒（1-3位）：`34:56.7`，`34:56.78`，`34:56.789`
   - 秒+毫秒（1-3位）：`56.7`，`56.78`，`56.789`

4. **节次**：
   - 中文：`第一节`, `第二节`, `第三节`, `第四节`，`第五节`, `第六节`, `第七节`, `第八节`，`第九节`, `第十节`，`加时赛`，`加时赛1-7`，`加时赛一-加时赛七`，`加时1-7`，`加时一-加时七`
   - 数字中文：`第1节`, `第2节`, `第3节`, `第4节`，`第5节`, `第6节`, `第7节`, `第8节`，`第9节`, `第10节`
   - 英文：`FIRST`, `SECOND`, `THIRD`, `FOURTH`，`OT`，`OVERTIME`，`OT1-OT7`，`OVERTIME1-OVERTIME7`
   - 缩写：`1st`, `2nd`, `3rd`, `4th`，`overtime`，`ot1-ot7`，`overtime1-overtime7`

### 图片命名

图片文件名可以是任意名称（如 `img001.jpg`, `shot001.jpg` 等），系统会自动从文件夹名中提取标签。

**注意**：
- 文件夹名必须包含在字符集中的字符
- 如果文件夹名包含不在字符集中的字符，该文件夹会被跳过
- 支持的图片格式：`.jpg`, `.jpeg`, `.png`, `.bmp`, `.tiff`, `.tif`

## 训练示例

### 基本训练

```bash
python train_LPRNet.py \
    --train_img_dirs ./data/train \
    --test_img_dirs ./data/test \
    --max_len 12 \
    --max_epoch 15 \
    --img_size 94,24
```

### 使用预训练模型继续训练

```bash
python train_LPRNet.py \
    --train_img_dirs ./data/train \
    --test_img_dirs ./data/test \
    --pretrained_model ./weights/Final_Scoreboard_model.pth \
    --resume_epoch 5 \
    --max_epoch 20
```

### 自定义参数训练

```bash
python train_LPRNet.py \
    --train_img_dirs ./data/train \
    --test_img_dirs ./data/test \
    --max_len 15 \
    --img_size 120,32 \
    --learning_rate 0.01 \
    --train_batch_size 64 \
    --max_epoch 20
```

## 测试示例

### 基本测试

```bash
python test_LPRNet.py \
    --test_img_dirs ./data/test \
    --pretrained_model ./weights/Final_Scoreboard_model.pth
```

### 显示测试结果（可视化）

```bash
python test_LPRNet.py \
    --test_img_dirs ./data/test \
    --pretrained_model ./weights/Final_Scoreboard_model.pth \
    --show true
```

### 单张图片测试（需要修改代码支持）

可以修改 `test_LPRNet.py` 添加单张图片测试功能。

## 支持的识别内容

### 1. 比分（3位数字）
- 示例：`123`, `045`, `999`
- 最大长度：3位

### 2. 24秒倒计时（2位数字）
- 示例：`24`, `15`, `05`
- 最大长度：2位

### 3. 倒计时
- **时分秒格式**：`12:34:56`
- **时分秒毫秒格式**：`12:34:56.789`
- 最大长度：12位（包含分隔符）

### 4. 节次
- **中文**：`第一节`, `第二节`, `第三节`, `第四节`
- **数字中文**：`第1节`, `第2节`, `第3节`, `第4节`
- **英文**：`FIRST`, `SECOND`, `THIRD`, `FOURTH`
- **缩写**：`1st`, `2nd`, `3rd`, `4th`

## 参数说明

### 训练参数

- `--max_len`: 最大文本长度（默认12）
- `--img_size`: 图片尺寸，格式为 `width,height`（默认 `94,24`）
- `--max_epoch`: 训练轮数（默认15）
- `--learning_rate`: 学习率（默认0.1）
- `--train_batch_size`: 训练批次大小（默认128）
- `--test_batch_size`: 测试批次大小（默认120）
- `--dropout_rate`: Dropout率（默认0.5）
- `--cuda`: 是否使用GPU（默认True）

### 测试参数

- `--show`: 是否显示测试结果（默认False）
- `--pretrained_model`: 预训练模型路径

## 注意事项

1. **图片尺寸**：建议根据实际计分板样式调整 `--img_size` 参数
2. **最大长度**：根据最长文本内容设置 `--max_len`，建议设置为12-15
3. **字符集**：如果遇到未支持的字符，需要修改 `data/load_data.py` 中的 `CHARS` 列表
4. **数据质量**：确保训练数据质量高，标签准确
5. **数据增强**：可以在数据加载器中添加数据增强以提高模型鲁棒性

## 常见问题

### Q: 如何识别其他格式的节次？

A: 修改 `data/load_data.py` 中的 `CHARS` 列表，添加所需的字符。

### Q: 图片尺寸应该设置为多少？

A: 建议根据实际计分板图片的宽高比设置。如果计分板文字较宽，可以增加宽度；如果文字较高，可以增加高度。

### Q: 训练时loss不下降怎么办？

A: 
1. 检查数据标签是否正确
2. 降低学习率
3. 增加训练数据量
4. 检查数据预处理是否正确

### Q: 如何提高识别准确率？

A:
1. 增加训练数据量
2. 提高数据质量
3. 调整模型参数（学习率、批次大小等）
4. 使用数据增强
5. 根据实际场景微调模型

